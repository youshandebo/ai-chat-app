# 服务器前端构建失败 - 修复指南

## 问题说明

你的部署在**前端构建**步骤失败了。这通常是由以下原因造成的：

1. **内存不足** - 编译大型 React 应用需要足够的内存
2. **磁盘空间不足** - 依赖项和构建文件需要空间
3. **Node.js 内存限制** - 默认的 Node 进程内存可能不够

---

## 🚀 快速修复（推荐）

在你的服务器上运行修复脚本：

```bash
cd /opt/ai-chat/ai-chat-app

# 运行修复脚本
bash scripts/fix-frontend-build.sh
```

这个脚本会：
1. ✅ 清理之前的构建文件
2. ✅ 优化系统内存
3. ✅ 使用增加的内存构建（2048-3072MB）
4. ✅ 如果失败，自动切换到无压缩构建
5. ✅ 验证构建结果
6. ✅ 重启服务

---

## 📊 当前服务器状态

根据你的服务器信息：

```
System load:  0.41
Memory usage: 28%
Swap usage:   6%
Available disk: 充足
```

**内存充足**，所以应该能成功构建。可能只是需要配置更高的内存限制。

---

## 手动修复步骤

如果修复脚本不起作用，按以下步骤手动修复：

### 1️⃣ 连接到服务器

```bash
ssh root@107.173.101.155
```

### 2️⃣ 进入项目目录

```bash
cd /opt/ai-chat/ai-chat-app/frontend
```

### 3️⃣ 清理旧的构建

```bash
rm -rf node_modules dist
npm cache clean --force
```

### 4️⃣ 重新安装依赖

```bash
npm install --legacy-peer-deps
```

### 5️⃣ 构建前端（使用更多内存）

```bash
# 尝试 2048MB 内存
NODE_OPTIONS="--max-old-space-size=2048" npm run build

# 如果还是失败，尝试 3072MB
NODE_OPTIONS="--max-old-space-size=3072" npm run build
```

### 6️⃣ 验证构建

```bash
# 检查构建输出
ls -la dist/

# 应该看到 index.html 和其他文件
```

### 7️⃣ 返回项目根目录

```bash
cd /opt/ai-chat/ai-chat-app

# 继续运行部署脚本的剩余步骤
bash scripts/deploy.sh
```

---

## 🔧 如果内存仍然不足

### 检查可用内存

```bash
free -h

# 示例输出:
#               total        used        free      shared  buff/cache   available
# Mem:           3.8Gi       1.0Gi       1.2Gi      100Mi       1.6Gi       2.5Gi
```

### 增加 Swap（虚拟内存）

```bash
# 创建 4GB swap 文件
sudo fallocate -l 4G /swapfile

# 设置权限
sudo chmod 600 /swapfile

# 创建 swap
sudo mkswap /swapfile

# 启用 swap
sudo swapon /swapfile

# 验证
swapon --show
```

### 再次尝试构建

```bash
cd /opt/ai-chat/ai-chat-app/frontend
NODE_OPTIONS="--max-old-space-size=2048" npm run build
```

---

## 💡 替代方案：使用 Docker 构建

如果内存问题无法解决，可以在本地构建然后上传：

### 本地构建（在你的开发机）

```bash
cd ai-chat-app/frontend
npm install
npm run build
```

### 上传到服务器

```bash
# 压缩构建文件
tar -czf frontend-dist.tar.gz frontend/dist

# 上传到服务器
scp frontend-dist.tar.gz root@107.173.101.155:/tmp/

# 在服务器上解压
ssh root@107.173.101.155
cd /opt/ai-chat/ai-chat-app
tar -xzf /tmp/frontend-dist.tar.gz
```

---

## 🐛 调试构建错误

如果看到具体的错误信息，可以尝试以下方法：

### 查看详细错误

```bash
cd /opt/ai-chat/ai-chat-app/frontend
npm run build 2>&1 | tee build.log

# 查看日志
cat build.log
```

### 常见错误和解决方案

**错误：`Cannot find module ...`**
```bash
rm -rf node_modules
npm install --legacy-peer-deps
```

**错误：`ENOMEM: Cannot allocate memory`**
```bash
NODE_OPTIONS="--max-old-space-size=4096" npm run build
```

**错误：`ENOSPC: no space left on device`**
```bash
# 检查磁盘
df -h
# 清理旧文件
sudo apt-get clean
npm cache clean --force
```

---

## ✅ 验证部署成功

修复后，确认应用运行正常：

```bash
# 检查进程
pm2 list

# 查看是否有两个进程运行:
# ai-chat-backend-6557
# ai-chat-frontend-6558

# 检查后端
curl http://localhost:6557/api/models

# 检查前端
curl http://localhost:6558
```

---

## 📞 如果还是不工作

收集以下信息后提交 Issue：

```bash
# 系统信息
uname -a
node -v
npm -v

# 内存信息
free -h
df -h

# 构建日志
cd /opt/ai-chat/ai-chat-app/frontend
NODE_OPTIONS="--max-old-space-size=2048" npm run build 2>&1 > build.log

# 上传 build.log
```

---

## 下次部署优化

为了避免这个问题，未来部署时：

1. **确保足够的内存**（至少 2GB 可用）
2. **检查磁盘空间**（至少 5GB 可用）
3. **使用最新版本的 Node.js**（20+ 推荐）
4. **考虑在内存充足的时间点部署**

---

## 相关文档

- [部署脚本说明](./部署脚本总结.md)
- [完整部署流程](./完整部署流程.md)
- [快速参考](./QUICK_REFERENCE.md)

---

**现在运行修复脚本：**

```bash
bash /opt/ai-chat/ai-chat-app/scripts/fix-frontend-build.sh
```

祝修复顺利！🚀
