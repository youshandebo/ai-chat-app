# 实际使用示例

## 📌 目录

1. [示例 1: 全新服务器部署](#示例-1-全新服务器部署)
2. [示例 2: 已有项目目录](#示例-2-已有项目目录)
3. [示例 3: 自定义端口](#示例-3-自定义端口)
4. [示例 4: 部署后操作](#示例-4-部署后操作)
5. [示例 5: 故障排查](#示例-5-故障排查)

---

## 示例 1: 全新服务器部署

### 场景
你已经有一个新的 Linux 服务器（Ubuntu 20.04+），需要从零开始部署应用。

### 步骤

#### 1️⃣ 连接到服务器

```bash
ssh root@your_server_ip
# 输入密码
```

#### 2️⃣ 一键部署

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat
```

#### 3️⃣ 脚本自动处理所有工作

脚本会输出类似这样的信息：

```
╔════════════════════════════════════════╗
║   🚀 AI Chat One-Click Deploy v2.0    ║
║   Clone → Check → Build → Start       ║
╚════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Step 1/8: Determine project directory
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[i] Need to clone from GitHub
[i] Clone project to: /opt/ai-chat/ai-chat-app
[✓] Project cloned

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Step 2/8: Check system environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✓] Node.js v20.10.0
[✓] npm 10.2.0
[✓] git 2.34.1

... (中间省略) ...

╔════════════════════════════════════════╗
║ ✓ AI Chat deployed and started!       ║
╚════════════════════════════════════════╝

📍 Access URLs:
   Frontend App:   http://localhost:6555
   Backend API:    http://localhost:6556
   Admin Panel:    http://localhost:6556/admin

🔑 Authentication:
   Admin Token: fnx081013fnx

📂 Project Directory:
   /opt/ai-chat/ai-chat-app
```

#### 4️⃣ 验证应用

```bash
# 查看 PM2 进程
pm2 list

# 输出应该显示:
# ┌─────┬──────────────────────┬─────────┬─────────┬───────────┐
# │ id  │ name                 │ version │ mode    │ status    │
# ├─────┼──────────────────────┼─────────┼─────────┼───────────┤
# │ 0   │ ai-chat-backend-6555 │ N/A     │ fork    │ online    │
# │ 1   │ ai-chat-frontend-6556│ N/A     │ fork    │ online    │
# └─────┴──────────────────────┴─────────┴─────────┴───────────┘

# 测试后端 API
curl http://localhost:6555/api/models
# 应该返回 JSON 格式的模型列表

# 测试前端
curl http://localhost:6556
# 应该返回 HTML 页面
```

#### 5️⃣ 访问应用

在浏览器中打开：

- 前端：`http://your_server_ip:6556`
- 管理面板：`http://your_server_ip:6556/admin` (Token: `fnx081013fnx`)

✅ **完成！应用已经运行！**

---

## 示例 2: 已有项目目录

### 场景
你已经有一个克隆好的项目目录，需要进行部署。

### 步骤

```bash
# 进入项目目录
cd /opt/ai-chat-app

# 运行部署脚本
bash scripts/deploy.sh

# 脚本会检测已有的项目，继续部署

# 输出:
# [i] Detected existing project directory
# [✓] Project directory: /opt/ai-chat-app
# (继续执行其他步骤...)
```

---

## 示例 3: 自定义端口

### 场景
你想要使用特定的端口号（比如 7000 和 7001）。

### 方法 A: 在部署时指定

```bash
# 使用端口 7000（后端）和 7001（前端）
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat 7000 7001

# 脚本会使用指定的端口启动
```

### 方法 B: 手动修改

```bash
# 编辑后端配置
nano /opt/ai-chat-app/backend/.env
# 修改: PORT=7000

# 编辑前端配置
nano /opt/ai-chat-app/frontend/.env
# 修改: VITE_BACKEND_BASE=http://localhost:7000

# 重启服务
pm2 restart all
```

---

## 示例 4: 部署后操作

### 添加 API Key

部署完成后，编辑环境变量添加 AI 模型的 API Key：

```bash
# 编辑后端配置
sudo nano /opt/ai-chat-app/backend/.env

# 添加 API Key:
GEMINI_API_KEY=sk-xxx-your-gemini-key-xxx
DEEPSEEK_API_KEY=sk-xxx-your-deepseek-key-xxx
CLAUDE_API_KEY=sk-xxx-your-claude-key-xxx

# 保存并退出 (Ctrl+O, Enter, Ctrl+X)

# 重启后端服务使配置生效
pm2 restart ai-chat-backend-6555
```

### 配置 Nginx 反向代理

如果想要通过单一入口访问：

```bash
# 设置 Nginx
sudo bash /opt/ai-chat-app/scripts/setup-nginx.sh yourshandebo.xx.kg 6555 6556 6557

# 检查 Nginx 状态
sudo systemctl status nginx

# 现在可以通过以下地址访问:
# http://yourshandebo.xx.kg:6557
```

### 配置 Cloudflare DNS

1. 登录 Cloudflare
2. 添加 A 记录：
   - 名称: `yourshandebo.xx.kg`
   - 内容: 你的服务器 IP
   - 状态: 已代理 ☁️

3. 现在可以通过以下地址访问：
   ```
   http://yourshandebo.xx.kg:6557
   ```

### 开启 PM2 开机自启

```bash
# 使用 PM2 官方启动脚本
pm2 startup

# 保存当前进程列表
pm2 save

# 重启服务器后，应用会自动启动
```

### 设置日志轮转

```bash
# 安装日志轮转插件
pm2 install pm2-logrotate

# 配置轮转策略
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 14
```

---

## 示例 5: 故障排查

### 问题 1: 克隆失败

```bash
# 错误信息:
# error: unable to access repository

# 解决方案 1: 检查网络
ping github.com

# 解决方案 2: 手动克隆
git clone https://github.com/youshandebo/ai-chat-app.git /opt/ai-chat
cd /opt/ai-chat
bash scripts/deploy.sh

# 解决方案 3: 使用代理（如需要）
git config --global url."https://github.com/".insteadOf git://github.com/
```

### 问题 2: Node.js 版本太低

```bash
# 错误: npm ERR! requires node >= 18

# 检查当前版本
node -v

# 升级 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证
node -v  # 应该是 20.x 或更高
```

### 问题 3: 构建失败 - 内存不足

```bash
# 错误: JavaScript heap out of memory

# 检查可用内存
free -h

# 增加 Node 内存限制
export NODE_OPTIONS="--max-old-space-size=2048"
bash scripts/deploy.sh

# 或者增加系统 swap
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 问题 4: 后端 API 无法访问

```bash
# 检查后端进程
pm2 list | grep backend

# 查看后端日志
pm2 log ai-chat-backend-6555

# 测试本地连接
curl http://localhost:6555/api/models

# 检查防火墙
sudo ufw status
sudo ufw allow 6555

# 检查端口
netstat -tln | grep 6555
lsof -i :6555
```

### 问题 5: 前端页面加载慢

```bash
# 检查前端进程
pm2 list | grep frontend

# 查看前端日志
pm2 log ai-chat-frontend-6556

# 检查系统资源
top
free -h
df -h

# 增加内存限制
pm2 restart ai-chat-frontend-6556 --max-memory-restart 512M
```

### 问题 6: 端口被占用

```bash
# 查看被占用的端口
lsof -i :6555

# 输出类似:
# COMMAND  PID USER   FD   TYPE  DEVICE SIZE/OFF NODE NAME
# node    1234 root   11u  IPv4    1234      0t0  TCP localhost:6555

# 杀死占用该端口的进程
kill -9 1234

# 或者使用不同的端口
bash scripts/deploy.sh . 8000 8001
```

### 问题 7: PM2 无法启动服务

```bash
# 检查 PM2 状态
pm2 status

# 查看错误日志
pm2 log --err

# 重新安装 PM2
sudo npm install -g pm2@latest

# 重启 PM2 守护进程
pm2 kill
pm2 resurrect
```

### 问题 8: 手动测试服务

```bash
# 进入项目目录
cd /opt/ai-chat-app

# 手动启动后端（调试模式）
cd backend
npm start

# 或者手动启动前端
cd ../frontend
node server.js

# 这样可以看到详细的错误消息，而不是 PM2 的隐藏输出
```

---

## 监控和维护

### 日常监控

```bash
# 查看所有进程状态
pm2 list

# 实时监控资源使用
pm2 monit

# 查看最近的日志
pm2 log | tail -50

# 查看后端最后 100 行日志
pm2 log ai-chat-backend-6555 --lines 100
```

### 定期维护

```bash
# 每周检查一次更新
cd /opt/ai-chat-app
git fetch origin
git log -1 --oneline origin/main

# 如果有新版本，更新
git pull origin main
bash scripts/deploy.sh

# 备份数据
tar -czf ~/backup/ai-chat-data-$(date +%Y%m%d).tar.gz backend/data/
```

### 定期清理

```bash
# 清理 PM2 日志
pm2 flush

# 清理系统缓存
sudo apt-get clean
sudo apt-get autoclean

# 检查磁盘使用
du -sh /opt/ai-chat-app
df -h
```

---

## 常用命令速查

```bash
# ========== 部署 ==========
bash scripts/deploy.sh                    # 部署/重新部署
bash scripts/diagnose.sh                  # 诊断环境

# ========== PM2 ==========
pm2 list                                  # 查看进程列表
pm2 log                                   # 查看实时日志
pm2 restart ai-chat-backend-6555         # 重启后端
pm2 restart ai-chat-frontend-6556        # 重启前端
pm2 stop all                              # 停止所有
pm2 delete all                            # 删除所有
pm2 monit                                 # 实时监控

# ========== 测试 ==========
curl http://localhost:6555/api/models    # 测试后端
curl http://localhost:6556               # 测试前端

# ========== 查看 ==========
pm2 log ai-chat-backend-6555              # 后端日志
pm2 log ai-chat-frontend-6556             # 前端日志

# ========== 配置 ==========
nano backend/.env                         # 编辑后端配置
nano frontend/.env                        # 编辑前端配置

# ========== 更新 ==========
git pull origin main                      # 获取最新代码
bash scripts/deploy.sh                    # 重新部署
```

---

## 完整的一天工作流程

### 早上 - 启动

```bash
# 连接到服务器
ssh root@server_ip

# 检查应用状态
pm2 list

# 查看最近的日志
pm2 log | tail -100
```

### 工作中 - 监控

```bash
# 监控资源使用
pm2 monit

# 或者定时检查
watch -n 5 'pm2 list'
```

### 下午 - 更新

```bash
# 如果有新代码
cd /opt/ai-chat-app
git pull origin main
bash scripts/deploy.sh
```

### 晚上 - 备份

```bash
# 备份数据
tar -czf ~/backup/ai-chat-data-$(date +%Y%m%d).tar.gz \
  /opt/ai-chat-app/backend/data/

# 清理日志
pm2 flush
```

---

## 完成！

现在你已经:
- ✅ 理解了一键部署脚本
- ✅ 学会了如何部署应用
- ✅ 掌握了故障排查方法
- ✅ 知道如何进行日常维护

准备好了吗？**立即部署！**

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat
```

祝你使用愉快！🚀
