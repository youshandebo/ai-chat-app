# 🚀 一键部署指南

## 什么是一键部署？

我们创建了一个完整的自动化部署脚本 `deploy.sh`，它会自动完成以下所有操作：

1. ✅ **从 GitHub 克隆项目** - 自动从仓库获取最新代码
2. ✅ **检查系统环境** - 验证 Node.js、npm、git 是否安装
3. ✅ **检查或安装 PM2** - 进程管理器
4. ✅ **自动分配端口** - 智能查找可用的 6555+ 端口
5. ✅ **构建后端** - TypeScript 编译
6. ✅ **构建前端** - React 应用构建
7. ✅ **配置环境** - 自动生成所有必需的配置文件
8. ✅ **启动服务** - PM2 自动管理和启动

**结果**: 只需运行一个命令，不到 5 分钟内应用就完全启动并运行！

---

## 快速开始（3 种方式）

### 方式 1️⃣: 完全自动部署（推荐 ⭐）

在服务器上直接运行这个命令，脚本自动克隆项目和部署：

```bash
# 克隆到 /opt 目录
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat
```

**这会自动：**
- 创建 `/opt/ai-chat` 目录
- 从 GitHub 克隆最新代码
- 自动查找可用端口（6555+）
- 构建并启动所有服务
- 显示访问 URL

---

### 方式 2️⃣: 指定自定义端口

如果想使用特定的端口号：

```bash
# 使用后端端口 6555，前端端口 6556
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat 6555 6556
```

---

### 方式 3️⃣: 已经克隆的项目

如果你已经手动克隆了项目，直接进入目录运行脚本：

```bash
cd /path/to/ai-chat-app
bash scripts/deploy.sh
```

---

## 部署完成后

脚本会输出一个完整的总结，包括：

```
╔════════════════════════════════════════╗
║ ✓ AI Chat deployed and started!       ║
╚════════════════════════════════════════╝

📍 Access URLs:
   Frontend App:   http://localhost:6555
   Backend API:    http://localhost:6556
   Admin Panel:    http://localhost:6556/admin

🔑 Authentication:
   Admin Token: fnx081013fnx

📂 Project Directory:
   /opt/ai-chat/ai-chat-app

⚙️  PM2 Process:
   ai-chat-backend-6555
   ai-chat-frontend-6556
```

### 立即验证

1. **打开浏览器** 访问前端应用：
   ```
   http://localhost:6556
   ```

2. **打开管理面板** 输入 Token：
   ```
   http://localhost:6556/admin
   Token: fnx081013fnx
   ```

3. **检查 API** 是否运行：
   ```bash
   curl http://localhost:6555/api/models
   ```

---

## 端口说明

脚本会自动分配以下端口（都遵循 6555+ 策略）：

| 端口 | 服务 | 用途 |
|------|------|------|
| 6555 | 后端 API | REST API 请求 |
| 6556 | 前端 Web | 用户界面 |
| 6557+ | Nginx (可选) | 反向代理 |

**所有端口都从 6555 开始向上递增，不使用 80/443**

---

## PM2 常用命令

部署完成后可以使用这些命令管理应用：

```bash
# 查看所有进程
pm2 list

# 查看实时日志
pm2 log

# 重启后端
pm2 restart ai-chat-backend-6555

# 重启前端
pm2 restart ai-chat-frontend-6556

# 停止所有服务
pm2 stop all

# 删除所有进程
pm2 delete all

# 实时监控
pm2 monit
```

---

## 如果出现问题

### 1. 克隆失败？

检查网络连接：

```bash
git clone https://github.com/youshandebo/ai-chat-app.git
```

如果仍然失败，手动克隆然后运行脚本：

```bash
git clone https://github.com/youshandebo/ai-chat-app.git /opt/ai-chat
cd /opt/ai-chat
bash scripts/deploy.sh
```

### 2. 环境检查失败？

确认系统已安装必需软件：

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install nodejs npm git

# CentOS/RHEL
sudo yum install nodejs npm git

# macOS
brew install node git
```

### 3. 端口被占用？

脚本会自动查找可用端口，但如果想要指定不同的端口：

```bash
bash scripts/deploy.sh . 7000 7001
```

### 4. 应用启动慢？

等待 30 秒让服务完全启动，然后检查日志：

```bash
pm2 log ai-chat-backend-6555
pm2 log ai-chat-frontend-6556
```

---

## 运行诊断

如果想要详细的环境检查和诊断报告：

```bash
cd /opt/ai-chat/ai-chat-app
bash scripts/diagnose.sh
```

这会检查：
- ✓ Node.js 和 npm 版本
- ✓ 项目结构
- ✓ 依赖安装
- ✓ 端口可用性
- ✓ 网络连接
- ✓ 服务健康状态

---

## 配置 Cloudflare（可选）

如果想要通过自定义域名访问：

1. **配置 Nginx 反向代理**：

   ```bash
   sudo bash scripts/setup-nginx.sh yourshandebo.xx.kg 6555 6556 6557
   ```

2. **在 Cloudflare 中添加 A 记录**：
   - 名称: `yourshandebo.xx.kg`
   - 内容: 你的服务器 IP
   - 状态: 已代理 ☁️

3. **访问应用**：
   ```
   http://yourshandebo.xx.kg:6557
   ```

详见 [CLOUDFLARE_SETUP.md](./CLOUDFLARE_SETUP.md)

---

## 更新应用

要更新到最新版本：

```bash
cd /opt/ai-chat/ai-chat-app
git pull origin main
bash scripts/deploy.sh
```

---

## 数据备份

定期备份数据目录：

```bash
# 查看数据文件
ls -la /opt/ai-chat/ai-chat-app/backend/data/

# 备份到其他地方
cp -r /opt/ai-chat/ai-chat-app/backend/data ~/backup/ai-chat-data-$(date +%Y%m%d)
```

---

## 完全卸载

如果想要移除应用：

```bash
# 停止所有服务
pm2 stop all
pm2 delete ai-chat-backend-6555 ai-chat-frontend-6556

# 删除应用目录
rm -rf /opt/ai-chat
```

---

## 总结

| 步骤 | 命令 | 时间 |
|------|------|------|
| 1️⃣ 运行脚本 | `bash deploy.sh /opt/ai-chat` | < 1 分钟 |
| 2️⃣ 克隆代码 | 自动 | 1-2 分钟 |
| 3️⃣ 检查环境 | 自动 | < 1 分钟 |
| 4️⃣ 构建项目 | 自动 | 2-3 分钟 |
| 5️⃣ 启动服务 | 自动 | < 1 分钟 |
| **总计** | **一个命令** | **< 5 分钟** |

✅ **完成！应用已运行**

---

## 需要帮助？

- 📖 查看完整文档: [README.md](./README.md)
- 🔧 快速参考: [QUICK_REFERENCE.md](./QUICK_REFERENCE.md)
- 📋 详细部署指南: [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)
- 🌐 Cloudflare 配置: [CLOUDFLARE_SETUP.md](./CLOUDFLARE_SETUP.md)
- 🐛 报告问题: https://github.com/youshandebo/ai-chat-app/issues

---

**祝部署顺利！** 🎉
