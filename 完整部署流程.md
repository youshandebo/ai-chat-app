# 完整部署工作流程

## 📋 目录

1. [快速开始](#快速开始)
2. [详细步骤](#详细步骤)
3. [常见场景](#常见场景)
4. [故障排查](#故障排查)

---

## 快速开始

### 一句话部署

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat
```

### 完成后

```bash
# 查看进程
pm2 list

# 查看日志
pm2 log

# 访问应用
# 前端: http://localhost:6556
# 管理面板: http://localhost:6556/admin (Token: fnx081013fnx)
```

---

## 详细步骤

### 步骤 1: 连接到服务器

```bash
ssh root@your_server_ip
```

### 步骤 2: 下载并运行部署脚本

#### 选项 A: 完全自动（推荐）

```bash
# 创建应用目录并部署
mkdir -p /opt
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat
```

#### 选项 B: 手动克隆后运行

```bash
# 克隆项目
git clone https://github.com/youshandebo/ai-chat-app.git /opt/ai-chat
cd /opt/ai-chat

# 运行部署脚本
bash scripts/deploy.sh
```

#### 选项 C: 使用自定义端口

```bash
bash scripts/deploy.sh /opt/ai-chat 6555 6556
```

### 步骤 3: 等待部署完成

脚本会输出：
- ✓ 环境检查
- ✓ 依赖安装
- ✓ 项目构建
- ✓ 服务启动
- ✓ 健康检查

### 步骤 4: 验证应用运行

```bash
# 检查进程
pm2 list

# 输出应该显示两个进程:
# ai-chat-backend-6555
# ai-chat-frontend-6556

# 测试后端 API
curl http://localhost:6555/api/models

# 测试前端
curl http://localhost:6556
```

### 步骤 5: 配置 Nginx（可选）

如果想要通过单一入口访问：

```bash
sudo bash /opt/ai-chat/scripts/setup-nginx.sh yourshandebo.xx.kg 6555 6556 6557
```

---

## 常见场景

### 场景 1: 部署到全新服务器

```bash
# 假设已安装 Node.js 20+, npm, git

ssh root@server_ip

# 一键部署
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat

# 完成！现在可以访问应用了
```

### 场景 2: 部署多个实例

```bash
# 实例 1: 端口 6555-6556
bash /opt/ai-chat-1/scripts/deploy.sh /opt/ai-chat-1 6555 6556

# 实例 2: 端口 6557-6558
bash /opt/ai-chat-2/scripts/deploy.sh /opt/ai-chat-2 6557 6558

# 使用 Nginx 负载均衡
sudo nano /etc/nginx/nginx.conf  # 配置上游服务器
```

### 场景 3: 更新现有部署

```bash
cd /opt/ai-chat

# 更新代码
git pull origin main

# 重新部署
bash scripts/deploy.sh

# 或者只重启服务
pm2 restart ai-chat-backend-6555 ai-chat-frontend-6556
```

### 场景 4: 迁移到新服务器

```bash
# 在新服务器上
bash <(curl -fsSL https://raw.githubusercontent.com/youshandebo/ai-chat-app/main/scripts/deploy.sh) /opt/ai-chat

# 恢复旧数据（如果有备份）
cp ~/backup/metrics.json /opt/ai-chat/backend/data/
pm2 restart ai-chat-backend-6555
```

### 场景 5: Docker 容器部署

创建 Dockerfile:

```dockerfile
FROM node:20-alpine

WORKDIR /app

# 克隆项目
RUN git clone https://github.com/youshandebo/ai-chat-app.git .

# 构建
RUN cd backend && npm install && npm run build
RUN cd ../frontend && npm install && npm run build && npm install express

# 启动
ENV PORT=3000 VITE_BACKEND_BASE=http://localhost:3000/api
CMD npm install -g pm2 && \
    pm2 start backend/dist/server.js --name backend --env PORT=6555 && \
    pm2 start frontend/server.js --name frontend --env PORT=6556 && \
    pm2 logs
```

运行:
```bash
docker build -t ai-chat .
docker run -p 6555:6555 -p 6556:6556 ai-chat
```

---

## 故障排查

### 问题 1: 无法连接到服务器

```bash
# 检查 SSH 连接
ssh -v root@server_ip

# 检查防火墙
sudo ufw status
sudo ufw allow 22/tcp

# 检查路由
traceroute server_ip
```

### 问题 2: Git 克隆失败

```bash
# 检查网络连接
ping github.com

# 使用 HTTPS 重试（比 SSH 更可靠）
git clone https://github.com/youshandebo/ai-chat-app.git

# 或者使用 SSH（需要配置 SSH key）
git clone git@github.com:youshandebo/ai-chat-app.git
```

### 问题 3: Node.js/npm 不可用

```bash
# 检查安装
node -v
npm -v

# 安装（Ubuntu/Debian）
sudo apt-get update
sudo apt-get install nodejs npm

# 安装（CentOS/RHEL）
sudo yum install nodejs npm

# 安装（使用 NVM）
curl https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
```

### 问题 4: 端口被占用

```bash
# 查看被占用的端口
lsof -i :6555
lsof -i :6556

# 强制杀死进程
kill -9 <PID>

# 或者使用不同的端口
bash scripts/deploy.sh . 7000 7001
```

### 问题 5: 构建失败

```bash
# 查看详细日志
cd backend
npm run build

# 或
cd ../frontend
npm run build

# 常见原因: 内存不足
free -h  # 检查可用内存

# 解决: 增加 swap
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 问题 6: PM2 启动失败

```bash
# 检查 PM2 状态
pm2 status

# 查看详细错误
pm2 log --err

# 手动启动测试
cd backend
npm start

# 或
cd ../frontend
node server.js
```

### 问题 7: 应用启动慢

```bash
# 检查系统资源
top
free -h
df -h

# 增加内存限制
pm2 start ... --max-memory-restart 1G

# 并发问题？降低 Node 进程数
export UV_THREADPOOL_SIZE=2
```

### 问题 8: 无法通过域名访问

```bash
# 1. 检查 DNS 解析
nslookup yourshandebo.xx.kg
dig yourshandebo.xx.kg

# 2. 检查防火墙
sudo ufw allow 6555
sudo ufw allow 6556
sudo ufw allow 6557

# 3. 检查 Nginx（如果使用）
sudo systemctl status nginx
sudo nginx -t

# 4. 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# 5. 测试本地连接
curl http://localhost:6555
curl http://localhost:6556
```

---

## 部署检查清单

- [ ] 服务器可访问
- [ ] Node.js 20+ 已安装
- [ ] npm 已安装
- [ ] git 已安装
- [ ] 脚本成功运行
- [ ] 后端进程运行中
- [ ] 前端进程运行中
- [ ] 后端 API 响应正常
- [ ] 前端网页加载正常
- [ ] 管理面板登录成功
- [ ] 防火墙开放了端口
- [ ] 域名已配置（如需要）
- [ ] 反向代理已配置（如需要）

---

## 性能优化

### 启用 PM2 日志轮转

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 14
```

### 启用 Gzip 压缩

在 Nginx 配置中：

```nginx
gzip on;
gzip_types text/plain application/json text/css application/javascript;
gzip_min_length 1024;
gzip_comp_level 6;
```

### 内存监控

```bash
# 启用 PM2 监控
pm2 monit

# 或设置内存限制
pm2 start ... --max-memory-restart 500M
```

---

## 自动启动（系统重启后）

```bash
# 让 PM2 开机自启
pm2 startup
pm2 save

# 或使用 systemd
sudo systemctl enable pm2-root
```

---

## 完全卸载

```bash
# 停止所有服务
pm2 stop all
pm2 delete all

# 删除应用目录
rm -rf /opt/ai-chat

# 移除 PM2（如需要）
pm2 unstartup
npm uninstall -g pm2
```

---

## 获取帮助

- 📖 [README](./README.md)
- 🚀 [一键部署](./一键部署.md)
- 🔧 [快速参考](./QUICK_REFERENCE.md)
- 🌐 [Cloudflare 设置](./CLOUDFLARE_SETUP.md)
- 🐛 [GitHub Issues](https://github.com/youshandebo/ai-chat-app/issues)

---

**祝部署成功！** 🎉
